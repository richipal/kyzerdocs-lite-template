# Dependencies
node_modules/

# Secrets
.env
.env.local

# Eval harness runtime artifacts (real production data — never committed)
evals/fixtures/
evals/reports/*.tmp.json

# plan 02-10 tuning sweep embedding cache — corpus embeddings, never credentials (T-02-10-04)
evals/reports/.tune-cache/

# OS
.DS_Store

# Agent worktrees (transient)
.claude/worktrees/

# Next.js build output
.next/
tsconfig.tsbuildinfo

# Widget bundle — regenerated by `npm run build:widget` (scripts/build-widget.mjs) on every build
public/widget.js

# Product runtime data (SQLite DB, uploads)
data/

# Packaged npm artifact
*.tgz
dist/
